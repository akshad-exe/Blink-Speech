export function calculateEAR(landmarks: { x: number; y: number }[]): number {
  // Gonna need to Replace with actual EAR calculation for eye landmarks and indices depend on MediaPipe
  return 0.3; // stub value, implement real math here
}
